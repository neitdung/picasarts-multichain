import React from 'react';
import {
    Center,
} from "@chakra-ui/react";

export default function NotInstall() {
    return (<Center h={'80vh'} bg="gray.100" fontSize={'2rem'} fontWeight={700}>Please install Metamask for using our product!</Center>);
}